rows, cols = [int(el) for el in input().split(", ")]

matrix = []
sum_total = 0
for _ in range(rows):
    inner_list = [int(el) for el in input().split(", ")]
    matrix.append(inner_list)
    sum_total += sum(inner_list)

print(sum_total)
print(matrix)
